# ATLAS Environment Configuration
# Copy this file to .env and fill in values as needed.

# Materials Project API key (optional — for cross-referencing)
# Get one free at: https://materialsproject.org/api
# MP_API_KEY=your_api_key_here

# Note: The core ATLAS pipeline uses JARVIS-DFT data which
# requires NO API key. The MP API key is only needed if you
# want to cross-reference results with Materials Project.
